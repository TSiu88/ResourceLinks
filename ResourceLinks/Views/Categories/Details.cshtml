@{
  Layout = "_Layout";
}

@model ResourceLinks.Models.Category;

<h2>Category Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Title): @Html.DisplayFor(model => model.Title)</h3>

@if(@Model.Links.Count == 0)
{
  <p>This category does not contain any links</p>
}
else
{
  <h4>The category contains next links:</h4>
  <ul>
  @foreach(var join in Model.Links)
  {
    <li>@join.Link.Description</li>
  }
  </ul>
}

<p>@Html.ActionLink("Edit Category", "Edit", new { id = Model.CategoryId })</p>
<p>@Html.ActionLink("Delete Category", "Delete", new { id = Model.CategoryId })</p>
<p>@Html.ActionLink("Back to categories", "Index")</p>